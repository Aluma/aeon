---
type: Log
---

### feature
- Branch: external
- **Repo:** Aluma/universal-context-hub
- **What:** Defined the VCH core architecture + local-first persistence contract for issue #3. Added ADR `docs/adr/0001-core-architecture.md` (five-stage pipeline, T0–T3 authority tiers, local-only/canonical/derived/never-overwritten boundary, conflict handling) and `docs/persistence.md`. Implemented `src/provenance.ts` (ProvenanceRecord schema + validation with T0/T1 cross-field invariants, deterministic eligibility with stale/conflict/low-confidence withholding, write-protection enforcement) and `src/persistence.ts` (ContextStore with atomic temp-file+fsync+rename writes, versioned load, ordered v1→v2 migration, refuse-newer-version). Added 31 tests (25 provenance unit + 6 persistence integration).
- **PR:** https://github.com/Aluma/universal-context-hub/pull/5
- **Why:** Issue #3 (ai-build, architecture, persistence) — pin the architecture/persistence contract before feature work so future PRs are hard to fake (Strategy Priority 1 & 4). `--fix-issues` run.
- **Verified locally:** `npm run ci` green — lint, typecheck, guard:skips, guard:placeholders, 50/50 tests (was 19). Fixed two TS strictness errors mid-build before pushing.
- **Known gap:** single-writer store (no locking/compaction yet), migration chain has one link (v1→v2), crash safety assumes POSIX atomic rename. All documented as out-of-scope in the PR.
- **Sandbox note:** git HTTPS push worked this run after `gh auth setup-git` (prior run had needed the Git Data API fallback). Also had to set a local git identity on the clone (`git -C ... config user.name/email`) since the clone had none.
