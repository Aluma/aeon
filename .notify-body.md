external-feature: Aluma/universal-context-hub — defined the VCH architecture + persistence contract (issue #3)

Pinned the core boundary before feature work so future PRs are hard to fake. Two docs and two source modules, all backed by tests:

- **ADR** (`docs/adr/0001-core-architecture.md`) — the five-stage pipeline (ingestion → canonical memory → derived context → retrieval → injection), T0–T3 authority tiers, and an explicit statement of what is local-only, canonical, derived, and never-silently-overwritten.
- **`src/provenance.ts`** — ProvenanceRecord schema with validation (T0 must be user-confirmed, T1 must be write-protected), deterministic eligibility that withholds stale / conflicting / low-confidence context, and write-protection enforcement.
- **`src/persistence.ts`** — a crash-safe local store: atomic temp-file + fsync + rename writes, versioned load, a proven v1→v2 migration, and refusal of newer/unknown versions.

Local CI green: lint, typecheck, both guards, 50/50 tests (was 19). No new dependencies, no cloud on the critical path.

PR: https://github.com/Aluma/universal-context-hub/pull/5
